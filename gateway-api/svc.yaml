apiVersion: v1
kind: Service
metadata:
  name: svc-gateway
  namespace: gateway-test
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: gateway-api-test
  type: CLusterIP
